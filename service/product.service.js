const product = require("../schema/product.schema");

const getProductList = () => {
  // `Product` 모델을 통해, MongoDB에서 데이터를 가져와야함.
  const productList = [];
  return ProductList;
};

module.exports = {
  getProductList,
};
